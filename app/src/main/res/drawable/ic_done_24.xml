<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="@integer/viewport_width"
    android:viewportHeight="@integer/viewport_height">

    <group
        android:name="@string/group_done_keyboard_arrow_left"
        android:pivotX="@integer/pivot_x"
        android:pivotY="@integer/pivot_y">

        <path
            android:name="@string/done"
            android:pathData="@string/path_done"
            android:strokeWidth="@integer/stroke_width"
            android:strokeLineCap="square"
            android:strokeColor="@color/stroke_color"/>
    </group>
</vector>